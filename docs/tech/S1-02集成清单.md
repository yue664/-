# H-ART-001 全流程演示集成（S1-02）

> 负责人：王码
> Sprint 1 D4
> 关联：event_player.gd + H-ART-001/剧本.json + PortraitCache

## 集成清单

- [x] EventPlayer.gd 支持 `portrait_switch` 指令（分镜内换立绘）
- [x] EventPlayer.gd 支持 `skippable` 字段（右上角跳过按钮）
- [x] AudioManager 集成 Edge TTS 音频加载
- [x] GameManager 结算调用（契约度/堕落值/道具）
- [x] PortraitCache 预加载当前事件涉及立绘
- [x] 幂等锁：EventManager.triggered_events 检查
- [ ] Win 手工冒烟（陈验 D5）
- [ ] Android 手工冒烟（陈验 D5）

## 结算流程

```
event_finished(H-ART-001) 
  → GameManager.on_event_finished("H-ART-001")
  → contract_artesia += 15（clamp 0-100）
  → fall_value += 10（clamp 0-100）
  → strength += 3
  → heroines["artesia"].unlocked = true
  → items.add("契约书")
  → SaveManager.auto_save()
```

## 触控降级

- Android 分镜 5 右上角"跳过"按钮，长按 500ms + 二次确认（R-018）
- 3D 演出关闭，仅立绘切换 + 场景背景（R-014）

## 变更日志

- 2024-09-13 D4：S1-02 集成清单就位